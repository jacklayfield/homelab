version: '3.8'

services:
  web:
    image: nginx:latest
    container_name: homelab-nginx
    ports:
      - "8080:80"
    volumes:
      - ../app:/usr/share/nginx/html:ro
    restart: unless-stopped