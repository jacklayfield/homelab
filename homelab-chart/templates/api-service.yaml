apiVersion: v1
kind: Service
metadata:
  name: {{ include "homelab.fullname" . }}-api
  labels:
    {{- include "homelab.labels" . | nindent 4 }}
    component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homelab.selectorLabels" . | nindent 4 }}
    component: api
