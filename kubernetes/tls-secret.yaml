# Self-signed TLS certificate for local development
# TODO Generate with:
# openssl req -x509 -newkey rsa:4096 -keyout tls.key -out tls.crt -days 365 -nodes -subj "/CN=homelab.local"
# kubectl create secret tls homelab-tls --cert=tls.crt --key=tls.key
#
# For now, this is a placeholder.

apiVersion: v1
kind: Secret
metadata:
  name: homelab-tls
  namespace: default
type: kubernetes.io/tls
data:
  # NOTE: These are placeholder values. TODO Need to generate my own certificate with:
  # openssl req -x509 -newkey rsa:4096 -keyout tls.key -out tls.crt -days 365 -nodes -subj "/CN=homelab.local"
  # Then encode to base64:
  # base64 -i tls.crt and base64 -i tls.key
  tls.crt: EXAMPLE
  tls.key: EXAMPLE
